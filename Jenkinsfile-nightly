pipeline {
    agent any
    stages ('Buidl Docker images') {
        steps {
            sh 'docker build -f ci/Dockerfile-playwright -t my-play-wright .'
            sh 'docker build -f ci/Dockerfile-aws-cli -t my-aws-cli .'
        }
    }
}